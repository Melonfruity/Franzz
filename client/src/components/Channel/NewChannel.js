import React from 'react';

const NewChannel = ({ joinChannel, createChannel }) => {
  return (
    <div>
      <section>
        JOIN A CHANNEL
      </section>
      <section>
        CREATE A CHANNEL
      </section>
    </div>
  );
};

export default NewChannel;
